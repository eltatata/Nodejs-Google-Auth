<div class="flex flex-col items-center gap-5 pt-10">
    <h1 class="text-3xl font-bold">Your Profile</h1>
    <div class="flex flex-col items-center gap-3">
        <img class="rounded-full" src="{{photos.[0].value}}" alt="image profile">
        <h3 class="text-xl font-semibold">{{displayName}}</h3>
        <p class="text-slate-600">{{emails.[0].value}}</p>
        <a class="border border-slate-500 rounded-lg px-2 py-1 hover:underline" href="/api/logout">Logout</a>
    </div>
</div>